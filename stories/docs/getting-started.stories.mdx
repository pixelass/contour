import { Meta } from "@storybook/addon-docs";
import Column from "../../packages/react/src/column";
import Grid from "../../packages/react/src/grid";
import { DebugBox } from "../helpers";

<Meta title="Docs/Getting Started" />

# Getting Started

## Install

```shell
# npm
npm install @contour/react --save;
# yarn
yarn add @contour/react --save
```

## Basic Usage

```tsx
import Grid from "@contour/react/grid";
import Column from "@contour/react/column";

export default function App() {
	return (
		<Grid>
			<Column>Contour</Column>
			<Column colSpan={{ xs: 2 }}>Contour</Column>
			<Column colSpan={{ xs: 2, m: 4 }}>Contour</Column>
		</Grid>
	);
}
```

<Grid>
	<Column>
		<DebugBox>Contour</DebugBox>
	</Column>
	<Column colSpan={{ xs: 2 }}>
		<DebugBox>Contour</DebugBox>
	</Column>
	<Column colSpan={{ xs: 2, m: 4 }}>
		<DebugBox>Contour</DebugBox>
	</Column>
</Grid>
